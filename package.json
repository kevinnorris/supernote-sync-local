{
  "name": "supernote-sync-local",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "fix": "xo --fix",
    "lint": "xo",
    "check": "bun run fix && bun run lint && bun test && bun run typecheck",
    "sync": "bun run index.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "eslint-plugin-check-file": "^3.3.1",
    "eslint-plugin-promise": "^7.2.1",
    "xo": "^2.0.2"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "pino": "^10.3.1",
    "zod": "^4.3.6"
  }
}
